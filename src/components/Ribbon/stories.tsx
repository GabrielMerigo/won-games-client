import { Meta, Story } from '@storybook/react';
import Ribbon from '.';

export default {
  title: `Ribbon`,
  component: Ribbon
} as Meta

export const RibbonStory: Story = () => <Ribbon>Ribbon</Ribbon>
