import { Meta, Story } from '@storybook/react';
import FormSign from '.';

export default {
  title: `Form/FormSign`,
  component: FormSign
} as Meta

export const FormSignStory: Story = () => (<div>
  <FormSign />
</div>)
