import { Meta, Story } from '@storybook/react';
import Checkbox from '.';

export default {
  title: `Checkbox`,
  component: Checkbox
} as Meta

export const CheckboxStory: Story = () => <Checkbox />
