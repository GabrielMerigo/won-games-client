import { render, screen } from '@testing-library/react'

import ShowCase from './'

describe('<ShowCase />', () => {
  //Quando tiver titulo deve aparacer titulos
  // quando tiver highlight deve aparecer highlight
  // Quando tiver heading deve aparecer heading
  it('', () => {})
})
